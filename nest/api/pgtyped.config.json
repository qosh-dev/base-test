{
  "srcDir": "src",
  "db": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "user": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}"
  },
  "include": ["src/modules/**/*.sql"],
  "emitTemplate": "{dir}/{name}.queries.ts"
}
